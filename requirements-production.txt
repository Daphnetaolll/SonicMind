# Production lightweight backend dependencies for SONICMIND_MODE=production_light.
# Render 2 GB build command: pip install -r requirements-production.txt
# Excludes torch, transformers, sentence-transformers, and faiss-cpu; use lexical retrieval only.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-dotenv>=1,<2
psycopg[binary]>=3.2,<4
psutil>=5.9,<8
stripe>=12,<14
